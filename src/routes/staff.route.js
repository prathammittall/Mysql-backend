import { Router } from "express";

const router = Router();

// Placeholder - to be implemented
router.route("/").get((req, res) => {
    res.json({ message: "Staff routes - to be implemented" });
});

export default router;
