import { Router } from "express";

const router = Router();

// Placeholder controller functions - to be implemented
router.route("/").get((req, res) => {
    res.json({ message: "Organiser routes - to be implemented" });
});

export default router;
